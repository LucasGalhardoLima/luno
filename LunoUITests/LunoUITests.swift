import XCTest

/// Placeholder for UI tests
/// UI test implementations will be added as part of individual user story tasks
final class LunoUITests: XCTestCase {
    func test_appLaunches() {
        let app = XCUIApplication()
        app.launch()
    }
}
